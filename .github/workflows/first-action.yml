name: First Workflow

on: workflow_dispatch

jobs:
  first-job:
    runs-on: ubuntu-latest
    steps: 
      - name: Multi-line print
        run: | 
          echo "First line!"
          echo "Second line!"
      - name: Print goodbye
        run: echo "Done - bye!"
